name: PR was labeled

on:
  pull_request:
    types: [labeled]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Debug context
        run: |
          echo "github.ref: ${{ github.ref }}"
          echo "github.head_ref: ${{ github.head_ref }}"
          echo "github.base_ref: ${{ github.base_ref }}"
          echo "github.event_name: ${{ github.event_name }}"
          echo "event.action: ${{ github.event.action || '' }}"
          echo "review_state: ${{ github.event.review.state || '' }}"

  call-on-label:
    uses: bth-algo/utils2/.github/workflows/on_label.yml@main
    with:
      course_id: ${{ vars.COURSE_ID2 }}
      available_assignments: ${{ vars.ASSIGNMENTS2 }}
      kmom_paths: ${{ vars.KMOM_PATHS2 }}
    secrets: inherit
